#!/usr/bin/env python

import sys

#for arg in reversed(sys.argv):
 
input = file("grades.csv")
count = -1
for line in input:
  if count != -1
   line = line.strip()
   myvec = line.split(',')
   count = count + float(myvec[2])
  else
   count = 0
print count